// This file is intentionally left empty
// Next.js provides all necessary type definitions by default

// Only add custom type declarations that aren't provided by Next.js
// For example, you can add global type extensions here:

declare global {
  // Add any global type extensions here
  // Example:
  // interface Window {
  //   myCustomProperty: SomeType;
  // }
}

export {}; // This makes the file a module
